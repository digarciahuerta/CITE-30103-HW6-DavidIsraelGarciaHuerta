all you need compile and run my C programs:

1.first you Compile with:
   gcc filename.c -o output

2. then you Run with:
   ./output

Example: if the file is `question23.c`, you would use:
   gcc question23.c -o question23
   ./question23
